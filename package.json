{
  "name": "spear-monorepo",
  "private": true,
  "description": "Spear monorepo workspace root",
  "license": "Apache-2.0",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/sunnypatneedi/spear"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "eval": "pnpm --filter @spear-secure/core eval",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
